name: Unit tests

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  unit-tests:
    name: Ubuntu / Debug
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Configure
        run: cmake --preset debug -DLLM_LAB_WARNINGS_AS_ERRORS=ON

      - name: Build unit-test targets
        run: >
          cmake --build --preset debug --parallel --target
          encode_decode model_io pretokenizer training dataset_test

      - name: Run unit tests
        run: >
          ctest --test-dir build/debug --output-on-failure
          -R '^(tokenizer\.|dataset\.unit$|python\.unit$)'
